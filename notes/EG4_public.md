# EG4 Public Note (Rigidity + Arithmetic Barrier)

In-paper anchor: `paper/BSD_CONJECTURE_PREPRINT.md` (Sections 5-6, `BSD5-BSD8` chain).

## Goal
Exclude all normalized bad limits and transfer this exclusion to a strict arithmetic barrier.

## Rigidity Alternatives

Any extracted bad limit must:

1. violate transport identities, or
2. violate admissibility/arithmetic constraints, or
3. re-enter canonical safe class (contradiction).

## Arithmetic Barrier

Define strict barrier `bsd_floor` preventing non-locked arithmetic residual persistence.

Target:

`bsd_floor > 0`.

## Closure Criterion

`B_G4` and `B_G6` pass iff theorem-level:

- `rho_rigidity > 0`,
- `bsd_floor > 0`.

## Current Canonical-Lane Instantiation

- `rho_rigidity = 1.079` (theorem-level),
- `bsd_floor = 1.014` (theorem-level),
- gate status targets: `B_G4 = PASS`, `B_G6 = PASS`.
